# JWT Configuration
# Secret key for signing JWT tokens (REQUIRED - minimum 32 characters)
JWT_SECRET=your-secret-key-min-32-chars-long-change-this-in-production

# Refresh Token Security
# Secret key for HMAC-SHA256 hashing of refresh tokens (REQUIRED - minimum 32 characters)
# This is used to securely hash refresh tokens before storing them in the database
REFRESH_TOKEN_SECRET=your-secret-key-min-32-chars-long-change-this-in-production-for-refresh-tokens

# Password Security
# Pepper value for password hashing (REQUIRED - minimum 16 characters)
# This adds an additional layer of security to password hashes beyond salting
PASSWORD_PEPPER=pepper-min-16-chars-change-in-prod

# Database Configuration
# SQLite database URL with read-write-create mode
DATABASE_URL=sqlite://auth.db?mode=rwc

# Audit Database Configuration
# This database stores security audit logs separately from application data
AUDIT_DATABASE_URL=sqlite://audit.db?mode=rwc

# Server Configuration (optional - defaults shown)
# HOST=0.0.0.0
# PORT=3000

# Application Logging Configuration
# Log level for application logs (default: INFO)
# Valid values: TRACE, DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# Optional: Write application logs to a file
# If not set, logs will only be written to console
APP_LOG_FILE=./logs/app.log

# Application log retention period in days (default: 7)
# Only applies when APP_LOG_FILE is set
APP_LOG_RETENTION_DAYS=7

# Audit log retention period in days (default: 90)
# Determines how long audit events are kept in the audit database
AUDIT_LOG_RETENTION_DAYS=90
